# 你的 Agent vs 研究前沿 Agent：差异总结与改进路线图

> 生成日期：2025-01-22
> 基于：Anthropic《Building Effective Agents》(2024.12)、Google A2A 协议、SWE-bench、AgentBench 等公开资料

---

## 一、一句话总结

**你的 Agent** 本质上是一个「增强型 LLM + 工具循环 + 人工审批工作流」的实用工具箱——LLM 能自主调工具解决开放问题，但关键决策（计划审批、步骤推进）始终由人把关。

**研究界说的 Agent** 则更进一步：它不仅要求 LLM 自主使用工具，还追求多智能体协作辩论、自动化质量门控（无需人等）、标准化基准评测（SWE-bench）、分层记忆检索，以及更长时间的自主运行能力。

简单说：你做的更像一个「给人用的增强版 Copilot」，研究前沿在探索「能独立工作、自我评估、多角色协作的自主智能体」。

---

## 二、七维度差异速览

| 维度 | 你的 Agent | 研究前沿 |
|------|-----------|---------|
| 架构哲学 | 零框架、直接调 API，实用主义 | Anthropic 也主张简单组合，但业界大量用 LangChain/LangGraph |
| 工作流模式 | Plan/Outline/Reflection 三种，缺 gate 和 routing | 五种模式全齐，强调自动化 gate 和分流 |
| 多智能体 | 单 Agent + 双角色串行讨论 | 多角色并行、辩论、层级委派、A2A 协议 |
| 记忆架构 | 单一 Markdown 文件 + grep | 向量库 + 情节/语义/程序分层记忆 |
| 评估体系 | 无内置 eval | SWE-bench、AgentBench 等标准化评测 |
| 自主程度 | 人审批计划 + 暂停/恢复 | 更长时间自主 + 人在回路检查点 |
| 工具协议 | MCP 已支持（前沿） | MCP + A2A 双协议生态 |

---

## 三、改进路线图（按投入产出比排序）

### 🥇 建议 ①：Plan 模式增加显式 Prompt Chaining + 自动 Gate

- **现状**：Plan 模式按步骤顺序执行，步骤之间无自动质量检查
- **改进**：步骤间插入轻量 gate——用便宜模型自动检查当前步骤输出是否达标，不通过自动重试
- **工作量**：**低**（约 2-3 天）
- **兼容性**：完全兼容，仅修改 `plan-core.js` 步骤推进逻辑

### 🥈 建议 ②：引入 Routing 机制——简单任务自动路由到便宜模型

- **现状**：所有任务用同一模型，简单查询也在消耗主力模型配额
- **改进**：入口处加路由层，便宜模型做意图分类，简单任务分流到便宜模型
- **工作量**：**低-中**（约 3-5 天）
- **兼容性**：完全兼容，在 `api-core.js` 入口加路由

### 🥉 建议 ③：多角色讨论增加 Voting / Parallelization 并行评估

- **现状**：Reflection 模式串行：学生→老师→学生→老师，单视角
- **改进**：3 个并行评估者（正确性/风格/安全）同时打分，汇总投票
- **工作量**：**中**（约 1-2 周）
- **兼容性**：架构兼容，`runAgentLoop` 天然支持并发

### ④ 建议 ④：项目记忆升级为向量检索（RAG）

- **现状**：单一 Markdown 文件 + grep 全文搜索
- **改进**：引入轻量向量库（LanceDB/ChromaDB），语义检索 + 现有文件并存
- **工作量**：**中-高**（约 2-4 周）
- **兼容性**：作为新工具加入，不替代现有系统

### ⑤ 建议 ⑤：接入 SWE-bench 做自我评估

- **现状**：无内置评估，改 prompt/模型只能凭感觉判断
- **改进**：接入 SWE-bench Lite，每次改 prompt 跑 mini-bench 回归测试
- **工作量**：**中**（约 1-2 周）
- **兼容性**：Agent 已具备所有必要工具（文件读写、命令执行、网页搜索）

---

## 四、优先级矩阵

```
                    工作量
              低        中        高
           ┌─────────┬─────────┬─────────┐
收益  高   │   ①     │   ③     │   ④     │
           ├─────────┼─────────┼─────────┤
      中   │   ②     │   ⑤     │         │
           ├─────────┼─────────┼─────────┤
      低   │         │         │         │
           └─────────┴─────────┴─────────┘
```

**建议实施顺序**：① → ② → ③ → ⑤ → ④

---

## 五、不改什么（守住优势）

以下是你 Agent 已经做得很好、不需要改的部分：

- ✅ **零框架、直接调 API**：与 Anthropic 的最佳实践一致，不要引入 LangChain
- ✅ **MCP 协议支持**：已经是前沿，继续保持
- ✅ **安全沙箱**：命令审批 + 工作目录限制，这是大多数研究原型不具备的工程优势
- ✅ **三种 Workflow 的灵活切换**：Plan / Outline / Reflection 覆盖了主要使用场景
- ✅ **前端零构建架构**：HTML/JS 直接可用，极低部署成本
